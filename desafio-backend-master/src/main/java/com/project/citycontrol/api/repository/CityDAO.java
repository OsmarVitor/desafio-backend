package com.project.citycontrol.api.models;

public interface CityDao {
    //repository.saveAll(cities);

    //repository.findAll()

    //repository.findByIbgeId(ibgeId) Obter os dados da cidade informando o id do IBGE;

    //repository.findCitiesByUf(uf) ordenada por nome

    //Retornar o nome do estado com a maior e menor quantidade de cidades e a
    //quantidade de cidades;

    //Retornar a quantidade de cidades por estado;

    //Dentre todas as cidades, obter as duas cidades mais distantes uma da outra com base
    //na localização (distância em KM em linha reta);


    //repository.save(city)

    //repository.findByIbgeId(ibgeId)

    //buscar por maop

}
